import React from "react";

const sourceRef = () => {
    // based on ref id  - return styled source + link to reading list.
    return (
        <div>
            
        </div>
    )
}

export default sourceRef;