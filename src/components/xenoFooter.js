import React from "react";

const XenoFooter = () => {
    return(
        <div className="scroll-container">
            <h3 className="scroll-text"> chapter 1: the sensorial || chapter 1: the sensorial ||</h3>
            <h3 className="scroll-text"> chapter 1: the sensorial || chapter 1: the sensorial ||</h3>
            <h3 className="scroll-text"> chapter 1: the sensorial || chapter 1: the sensorial ||</h3>
            <h3 className="scroll-text"> chapter 1: the sensorial || chapter 1: the sensorial ||</h3>
            <h3 className="scroll-text"> chapter 1: the sensorial || chapter 1: the sensorial ||</h3>
        </div>
    )
};

export default XenoFooter;
